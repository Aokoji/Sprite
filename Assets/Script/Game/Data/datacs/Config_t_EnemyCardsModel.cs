using System.Collections.Generic;using UnityEditor;using System;public class Config_t_EnemyCardsModel : AssetData{	public List<t_EnemyCardsModel> asset = new List<t_EnemyCardsModel>();	public void addContent(t_EnemyCardsModel item)	{		asset.Add(item);	}	public static bool isloaded;	public static Dictionary<int, t_EnemyCardsModel> _data { get; private set; }	public void init()	{		_data = new Dictionary<int, t_EnemyCardsModel>();		for (int i = 0; i < asset.Count; i++){			_data.Add(asset[i].id, asset[i]);		}		asset = null;		isloaded = true;	}	public static t_EnemyCardsModel getOne(int id)	{		if (_data.ContainsKey(id))			return _data[id];		else			throw new Exception("not find" + id);	}	public static void Dispose()	{		_data = null;	}}[Serializable]public class t_EnemyCardsModel{	public int id;	public string cardsname;	public string cardlist;}